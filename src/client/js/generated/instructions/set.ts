/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from 'gill';
import { WS_VESTING_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_DISCRIMINATOR = new Uint8Array([
  198, 51, 53, 241, 116, 29, 126, 194,
]);

export function getSetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_DISCRIMINATOR);
}

export type SetInstruction<
  TProgram extends string = typeof WS_VESTING_PROGRAM_ADDRESS,
  TAccountWsVesting extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWsVesting extends string
        ? WritableAccount<TAccountWsVesting>
        : TAccountWsVesting,
      ...TRemainingAccounts,
    ]
  >;

export type SetInstructionData = {
  discriminator: ReadonlyUint8Array;
  value: number;
};

export type SetInstructionDataArgs = { value: number };

export function getSetInstructionDataEncoder(): FixedSizeEncoder<SetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['value', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_DISCRIMINATOR })
  );
}

export function getSetInstructionDataDecoder(): FixedSizeDecoder<SetInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['value', getU8Decoder()],
  ]);
}

export function getSetInstructionDataCodec(): FixedSizeCodec<
  SetInstructionDataArgs,
  SetInstructionData
> {
  return combineCodec(
    getSetInstructionDataEncoder(),
    getSetInstructionDataDecoder()
  );
}

export type SetInput<TAccountWsVesting extends string = string> = {
  ws-vesting: Address<TAccountWsVesting>;
  value: SetInstructionDataArgs['value'];
};

export function getSetInstruction<
  TAccountWsVesting extends string,
  TProgramAddress extends Address = typeof WS_VESTING_PROGRAM_ADDRESS,
>(
  input: SetInput<TAccountWsVesting>,
  config?: { programAddress?: TProgramAddress }
): SetInstruction<TProgramAddress, TAccountWsVesting> {
  // Program address.
  const programAddress = config?.programAddress ?? WS_VESTING_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    ws-vesting: { value: input.ws-vesting ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.ws-vesting)],
    programAddress,
    data: getSetInstructionDataEncoder().encode(args as SetInstructionDataArgs),
  } as SetInstruction<TProgramAddress, TAccountWsVesting>;

  return instruction;
}

export type ParsedSetInstruction<
  TProgram extends string = typeof WS_VESTING_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    ws-vesting: TAccountMetas[0];
  };
  data: SetInstructionData;
};

export function parseSetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      ws-vesting: getNextAccount(),
    },
    data: getSetInstructionDataDecoder().decode(instruction.data),
  };
}
